// interactive.js - added 10/14
/*document.addEventListener('DOMContentLoaded', function () {
  // Attach click event listeners to all elements with the class 'task'
  const tasks = document.querySelectorAll('.task');
  tasks.forEach(task => {
    task.addEventListener('click', () => {
      const impact = task.nextElementSibling; // Assumes the 'impact' is right after the 'task'
      if (impact && impact.classList.contains('impact')) {
        impact.style.display = impact.style.display === 'none' ? 'inline' : 'none'; // Toggle display
      }
    });
  });
});
*/
